[package]
name = "clone_engine_core"
version = "0.1.0-beta.1"
edition = "2021"
license = "GPL-3.0-only"
description = "Host-testable logic shared by the SSBU Clone Engine, with no skyline dependency"

[lib]
crate-type = ["rlib"]

[dependencies]
